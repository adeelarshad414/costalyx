{{- if .Values.worker.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "costalyx.workerName" . }}
  labels:
    {{- include "costalyx.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "costalyx.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "costalyx.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      serviceAccountName: {{ include "costalyx.workerServiceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: worker
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          workingDir: /app/backend
          command: ["node", "dist/worker.js"]
          env:
            - name: NODE_ENV
              value: production
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.databaseUrlKey }}
            - name: VAULT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.vaultTokenKey }}
            - name: COSTALYX_CLOUD_SCHEDULER_ENABLED
              value: {{ .Values.worker.scheduler.enabled | quote }}
            - name: COSTALYX_CLOUD_SCHEDULER_INTERVAL_MS
              value: {{ .Values.worker.scheduler.intervalMs | quote }}
            - name: COSTALYX_CLOUD_SCHEDULER_INGESTION_ENABLED
              value: {{ .Values.worker.scheduler.ingestionEnabled | quote }}
            - name: COSTALYX_BILLING_AGENT_TENANT_IDS
              value: {{ .Values.worker.scheduler.billingAgentTenantIds | quote }}
            - name: COSTALYX_BILLING_AGENT_ANOMALY_SCAN_ENABLED
              value: {{ .Values.worker.scheduler.billingAgentAnomalyScanEnabled | quote }}
            - name: COSTALYX_BILLING_AGENT_STATEMENT_GENERATION_ENABLED
              value: {{ .Values.worker.scheduler.billingAgentStatementGenerationEnabled | quote }}
            - name: COSTALYX_BILLING_AGENT_STATEMENT_SEND_ENABLED
              value: {{ .Values.worker.scheduler.billingAgentStatementSendEnabled | quote }}
            - name: COSTALYX_BILLING_AGENT_STATEMENT_SEND_LIMIT
              value: {{ .Values.worker.scheduler.billingAgentStatementSendLimit | quote }}
            - name: COSTALYX_BILLING_AGENT_PERIOD_START
              value: {{ .Values.worker.scheduler.billingAgentPeriodStart | quote }}
            - name: COSTALYX_BILLING_AGENT_PERIOD_END
              value: {{ .Values.worker.scheduler.billingAgentPeriodEnd | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "costalyx.fullname" . }}-config
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
